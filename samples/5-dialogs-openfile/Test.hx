import systools.Dialogs;

class Test {
		
	static function main() {
		trace(Dialogs.openFile("Select a file please!", "Please select one or more files, so we can see if this method works", "") );		
	}
}
